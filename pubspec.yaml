name: traveling_through_space
description: "A Flutter app simulating space travel between stars."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^2.0.0


flutter:
  uses-material-design: true